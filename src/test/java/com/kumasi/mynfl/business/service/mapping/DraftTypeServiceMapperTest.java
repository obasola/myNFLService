/*
 * Created on 5 Jun 2016 ( Time 14:55:36 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.kumasi.mynfl.business.service.mapping;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.modelmapper.ModelMapper;
import org.modelmapper.convention.MatchingStrategies;
import org.springframework.stereotype.Component;
import com.kumasi.mynfl.domain.DraftType;
import com.kumasi.mynfl.domain.jpa.DraftTypeEntity;
import com.kumasi.mynfl.test.MockValues;

/**
 * Test : Mapping between entity beans and display beans.
 */
public class DraftTypeServiceMapperTest {

	private DraftTypeServiceMapper draftTypeServiceMapper;

	private static ModelMapper modelMapper = new ModelMapper();

	private MockValues mockValues = new MockValues();
	
	
	@BeforeClass
	public static void setUp() {
		modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.STRICT);
	}
	
	@Before
	public void before() {
		draftTypeServiceMapper = new DraftTypeServiceMapper();
		draftTypeServiceMapper.setModelMapper(modelMapper);
	}
	
	/**
	 * Mapping from 'DraftTypeEntity' to 'DraftType'
	 * @param draftTypeEntity
	 */
	@Test
	public void testMapDraftTypeEntityToDraftType() {
		// Given
		DraftTypeEntity draftTypeEntity = new DraftTypeEntity();
		draftTypeEntity.setCode(mockValues.nextString(1));
		draftTypeEntity.setName(mockValues.nextString(45));
		
		// When
		DraftType draftType = draftTypeServiceMapper.mapDraftTypeEntityToDraftType(draftTypeEntity);
		
		// Then
		assertEquals(draftTypeEntity.getCode(), draftType.getCode());
		assertEquals(draftTypeEntity.getName(), draftType.getName());
	}
	
	/**
	 * Test : Mapping from 'DraftType' to 'DraftTypeEntity'
	 */
	@Test
	public void testMapDraftTypeToDraftTypeEntity() {
		// Given
		DraftType draftType = new DraftType();
		draftType.setCode(mockValues.nextString(1));
		draftType.setName(mockValues.nextString(45));

		DraftTypeEntity draftTypeEntity = new DraftTypeEntity();
		
		// When
		draftTypeServiceMapper.mapDraftTypeToDraftTypeEntity(draftType, draftTypeEntity);
		
		// Then
		assertEquals(draftType.getCode(), draftTypeEntity.getCode());
		assertEquals(draftType.getName(), draftTypeEntity.getName());
	}

}
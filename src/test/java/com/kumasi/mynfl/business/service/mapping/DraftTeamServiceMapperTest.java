/*
 * Created on 5 Jun 2016 ( Time 14:55:36 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.kumasi.mynfl.business.service.mapping;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.modelmapper.ModelMapper;
import org.modelmapper.convention.MatchingStrategies;
import org.springframework.stereotype.Component;
import com.kumasi.mynfl.domain.DraftTeam;
import com.kumasi.mynfl.domain.jpa.DraftTeamEntity;
import com.kumasi.mynfl.domain.jpa.DraftEntity;
import com.kumasi.mynfl.test.MockValues;

/**
 * Test : Mapping between entity beans and display beans.
 */
public class DraftTeamServiceMapperTest {

	private DraftTeamServiceMapper draftTeamServiceMapper;

	private static ModelMapper modelMapper = new ModelMapper();

	private MockValues mockValues = new MockValues();
	
	
	@BeforeClass
	public static void setUp() {
		modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.STRICT);
	}
	
	@Before
	public void before() {
		draftTeamServiceMapper = new DraftTeamServiceMapper();
		draftTeamServiceMapper.setModelMapper(modelMapper);
	}
	
	/**
	 * Mapping from 'DraftTeamEntity' to 'DraftTeam'
	 * @param draftTeamEntity
	 */
	@Test
	public void testMapDraftTeamEntityToDraftTeam() {
		// Given
		DraftTeamEntity draftTeamEntity = new DraftTeamEntity();
		draftTeamEntity.setTeamCode(mockValues.nextString(3));
		draftTeamEntity.setTeamName(mockValues.nextString(45));
		draftTeamEntity.setDraft(new DraftEntity());
		draftTeamEntity.getDraft().setId(mockValues.nextInteger());
		
		// When
		DraftTeam draftTeam = draftTeamServiceMapper.mapDraftTeamEntityToDraftTeam(draftTeamEntity);
		
		// Then
		assertEquals(draftTeamEntity.getTeamCode(), draftTeam.getTeamCode());
		assertEquals(draftTeamEntity.getTeamName(), draftTeam.getTeamName());
		assertEquals(draftTeamEntity.getDraft().getId(), draftTeam.getDraftId());
	}
	
	/**
	 * Test : Mapping from 'DraftTeam' to 'DraftTeamEntity'
	 */
	@Test
	public void testMapDraftTeamToDraftTeamEntity() {
		// Given
		DraftTeam draftTeam = new DraftTeam();
		draftTeam.setTeamCode(mockValues.nextString(3));
		draftTeam.setTeamName(mockValues.nextString(45));
		draftTeam.setDraftId(mockValues.nextInteger());

		DraftTeamEntity draftTeamEntity = new DraftTeamEntity();
		
		// When
		draftTeamServiceMapper.mapDraftTeamToDraftTeamEntity(draftTeam, draftTeamEntity);
		
		// Then
		assertEquals(draftTeam.getTeamCode(), draftTeamEntity.getTeamCode());
		assertEquals(draftTeam.getTeamName(), draftTeamEntity.getTeamName());
		assertEquals(draftTeam.getDraftId(), draftTeamEntity.getDraft().getId());
	}

}